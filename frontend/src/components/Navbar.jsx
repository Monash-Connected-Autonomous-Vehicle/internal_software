import React from 'react';

const NavBar = () => {
  const style = {
    height: '80px',
    position: 'fixed',
    width: '100%',
    top: 0,
    backgroundColor: 'black',
    color: 'white'
  };

  return (
    <div style={style}>
      MONASH CONNECTED AUTONOMOUS VEHICLE -- -- PLACEHOLDER NAVBAR -- --
    </div>
  );
}

export default NavBar;